<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="css/styles.css" />
        <link rel="stylesheet" href="css/bootstrap-rtl.css" />
        <link rel="stylesheet" href="css/font-awesome.css" />
        <title><%= pageTitle %></title>
    </head>
    <body class="rtl">
        <div class="form-group">
            <h1>کارهای <small>روزمره</small></h1>
            <form
                action="/admin/add-todo"
                role="form"
                class="row w-50 justify-content-center mx-auto"
                method="POST"
            >
                <div class="input-group">
                    <input
                        type="text"
                        name="todo"
                        class="form-control w-50"
                        placeholder="کار امروز"
                    />
                    <div class="input-group-prepend">
                        <button type="submit" class="btn btn-primary fa fa-plus"/>
                    </div>
                </div>
            </form>
            <div class="mx-auto w-50 mb-3 alert alert-primary row">
                <p class="d-inline col">کارهای انجام شده : <span class="badge badge-success badge-pill"><%= completeTodo %> </span></p>
                <p class="d-inline col">کارهای باقی مانده امروز : <span class="badge badge-warning badge-pill"><%= remainingTodo %> </span></p>
            </div>
            <% if(todos.length > 0) {%>
              <ul class="list-unstyled" id="todo">
                <% for(let todo of todos) {%>
                  <li>
                    <div class="alert <%= todo.complated ? 'alert-success' : 'alert-secondary' %> ">
                      <% if(todo.complated) { %>
                        <del><%= todo.text %></del>
                        <a href="/admin/delete-todo/<%= todo.id %>" class="close fa fa-trash text-danger"></a>
                      <% } else { %>
                        <%= todo.text %>
                        <a href="/admin/delete-todo/<%= todo.id %>" class="close fa fa-trash text-danger"></a>
                        <a href="/admin/completed-todo/<%= todo.id %>" class="close fa fa-check-square text-success mr-2"></a>
                      <% } %> 
                    </div>
                  </li>
                <% } %>
              </ul>
            <% } else { %>
              <div class="alert alert-warning w-50 mx-auto">
                <h4 class="alert-heading">
                  هیچ کاری برای امروز نیست
                </h4>

                <p>
                  می تونی کار های امروزت رو بالا بنویسی تا برات ثبت کنم
                </p>

                <hr>

                <p class="mb-0">
                  اگه کم و کاستی بود به دل نگیر

                  <i class="fa fa-heart text-danger"></i>
                </p>
              </div>
            <% } %> 
        </div>
    </body>
</html>
